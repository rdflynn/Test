using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class sjh : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //hello
        //hello from second lab
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
